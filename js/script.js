const progress=document.getElementById('progress'), header=document.getElementById('header'), nav=document.getElementById('nav'), menuToggle=document.getElementById('menuToggle');
function onScroll(){const d=document.documentElement;progress.style.width=((d.scrollTop)/(d.scrollHeight-d.clientHeight)*100)+'%';header.classList.toggle('scrolled',scrollY>20)}addEventListener('scroll',onScroll,{passive:true});onScroll();
menuToggle?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuToggle.setAttribute('aria-expanded',String(open))});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menuToggle?.setAttribute('aria-expanded','false')}));
const revealObs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');revealObs.unobserve(e.target)}}),{threshold:.12,rootMargin:'0px 0px -35px'});document.querySelectorAll('.reveal:not(.visible)').forEach(el=>revealObs.observe(el));
const roles=['AWS & DevOps Engineer','AWS Cloud Engineer','Linux Administrator','Cloud Infrastructure | CI/CD'];let ri=0,ci=0,del=false;const tw=document.getElementById('typewriter');function typeLoop(){if(!tw)return;const s=roles[ri];if(!del){ci++;tw.textContent=s.slice(0,ci);if(ci===s.length){del=true;return setTimeout(typeLoop,1700)}}else{ci--;tw.textContent=s.slice(0,ci);if(ci===0){del=false;ri=(ri+1)%roles.length}}setTimeout(typeLoop,del?38:62)}typeLoop();
const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;document.querySelectorAll('.tilt').forEach(card=>{if(reduce)return;card.addEventListener('mousemove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(900px) rotateX(${y*-5}deg) rotateY(${x*5}deg) translateY(-6px)`});card.addEventListener('mouseleave',()=>card.style.transform='')});
(function(){const c=document.getElementById('network');if(!c||reduce)return;const x=c.getContext('2d');let w,h,nodes=[];function size(){w=c.width=innerWidth;h=c.height=innerHeight;nodes=Array.from({length:Math.min(50,Math.max(22,Math.floor(w*h/30000)))},()=>({x:Math.random()*w,y:Math.random()*h,vx:(Math.random()-.5)*.24,vy:(Math.random()-.5)*.24}))}function draw(){x.clearRect(0,0,w,h);nodes.forEach(n=>{n.x+=n.vx;n.y+=n.vy;if(n.x<0||n.x>w)n.vx*=-1;if(n.y<0||n.y>h)n.vy*=-1});for(let i=0;i<nodes.length;i++)for(let j=i+1;j<nodes.length;j++){const a=nodes[i],b=nodes[j],dx=a.x-b.x,dy=a.y-b.y,d=Math.hypot(dx,dy);if(d<145){x.strokeStyle=`rgba(25,200,255,${.14*(1-d/145)})`;x.beginPath();x.moveTo(a.x,a.y);x.lineTo(b.x,b.y);x.stroke()}}nodes.forEach(n=>{x.fillStyle='rgba(133,223,255,.58)';x.beginPath();x.arc(n.x,n.y,1.5,0,Math.PI*2);x.fill()});requestAnimationFrame(draw)}size();draw();addEventListener('resize',size,{passive:true})})();
document.getElementById('year').textContent=new Date().getFullYear();
